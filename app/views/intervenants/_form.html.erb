<%= bootstrap_form_for(@intervenant) do |f| %>
  <% if @intervenant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@intervenant.errors.count, "error") %> prohibited this intervenant from being saved:</h2>

      <ul>
      <% @intervenant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-sm-4">
 
      <%= f.text_field :nom %>
      <%= f.text_field :prenom, label:"Prénom" %>
      <br>
      
      <%= f.submit %>

      <%= link_to '| Retour', intervenants_path %>
    </div>
  </div>  

<% end %>

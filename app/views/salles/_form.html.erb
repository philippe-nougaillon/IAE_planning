<%= bootstrap_form_for(@salle) do |f| %>
  <% if @salle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salle.errors.count, "error") %> prohibited this salle from being saved:</h2>

      <ul>
      <% @salle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-sm-4">

      <%= f.text_field :nom %>
      <%= f.number_field :places %>
      
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>  
<% end %>

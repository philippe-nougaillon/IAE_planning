      <hr>
      <h4>
        <%= l(cours.debut, format: :heures_min) %>
        <small>- <%= l(cours.fin, format: :heures_min) %></small>
        <div class="pull-right">
          <% if cours.salle %>
            <span class="label <%= cours.style %>">
              <%= cours.salle.nom %>
            </span>
          <% end %>
        </div>
      </h4>

      <b><%= cours.formation.nom %></b><br>
      <%= cours.intervenant.nom_prenom %><br>
      <% if cours.intervenant_binome %>
        <br>
        <%= cours.intervenant_binome.nom_prenom %>
      <% end %>

      <% if cours.url? && cours.elearning?  %>
          <%= link_to cours.nom.split(' ').first, target:"_blank" do %>
              <%= cours.nom.split(' ').last %>
              <span class="glyphicon glyphicon-link"></span>
          <% end %>
      <% else %>         
          <i><%= cours.nom_ou_ue %></i>
      <% end %>
      
      <% if (cours.debut.to_date == Date.today && cours_progress_bar_pct > 0 && cours_progress_bar_pct < 100) %> 
        <br>
        <div class="progress" style="width: 100%">
          <div class="progress-bar progress-bar-striped active" 
               role="progressbar" aria-valuemin="0" aria-valuemax="100" 
               aria-valuenow="<%= cours_progress_bar_pct %>" 
               style="width:<%= cours_progress_bar_pct %>%">
          </div>
        </div>
      <% end %>

<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>
        Votre cours <%= "#{@cours.formation.nom}/#{@cours.nom_ou_ue}" %> 
        du <%= l(@cours.debut, format: :long) %> 
        vient d'être <b><%= @cours.etat.humanize %></b>
    </h1>
    <p>
      <%= link_to "Se connecter au planning", cours_url(view: "calendar_rooms", start_date:@cours.debut) %>
    </p>
  </body>
</html>